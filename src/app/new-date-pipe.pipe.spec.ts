import { NewDatePipePipe } from './new-date-pipe.pipe';

describe('NewDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NewDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
